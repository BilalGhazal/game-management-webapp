<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <header>
        <h1>GameOn</h1>
    </header>
    <div>
        <% data.forEach(datum => { %>
            <img src="<%= datum.image_url %>">
            <div>Title: <%= datum.title %></div>
            <div>Deverloper: <%= datum.name %></div>
            <div>Genres: <%= datum.genre_names.join(", ") %></div>
        <% }) %>
    </div>
</body>
</html>